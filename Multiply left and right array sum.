int multiply(int arr[], int n)
{
    int sum1 = 0;
    int sum2 = 0;
    int half = n/2;
    for(int i = 0;i<half;i++)
    {
        sum1 = sum1 + arr[i];
    }
    for(int i = n-1;i>=half;i--)
    {
        sum2 = sum2 + arr[i];
    }
    return sum1*sum2;
}
