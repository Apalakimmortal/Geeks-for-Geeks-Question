class Solution{
public:
	int search(int arr[], int n, int k) {
	    int ans = -1;;
	    for(int i = 0;i<n;i++)
	    {
	        if(k == arr[i])
	        {
	            ans = i + 1;
	            break;
	        }
	    }
	    return ans ;
	    
	}
};
